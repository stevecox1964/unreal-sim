# Handoff — Play phase underway: travel + rest verified live, OpenRouter qwen cuts run cost, #45 speech fix awaiting SR66

**Date:** 2026-09-22 19:25 America/Chicago
**Branch:** main
**Prior handoff:** [HANDOFF_2026-09-22_1200.md](HANDOFF_2026-09-22_1200.md)

## TL;DR

Survey is closed; the Play ladder (MASTER_PLAN Part IV, backlog Active view) is the plan. P0
(place travel) and P1 (#27 A false stuck) passed live. Runs now use **OpenRouter
`qwen/qwen3.7-flash`** — SR65 ran 17 min / 227 ticks with only 13 LLM decisions, so long runs are
cheap. SR65 reproduced #45 (Maren greeted a settled Dufus; he never heard it); the fix is in but
**not yet run live**. Next: user runs **SR66** (≥15 min, past 10:30 sim time); Claude reads logs.

## What was done this session

- **Planning/docs:** MASTER_PLAN "Where we are" + Part IV current state + Play ladder P0–P5;
  backlog Active view 2026-09-22 with run log SR61–SR65; items #107 (test rot), #108 (local
  Ollama, parked), #109 (mannequins), #110 (OpenRouter), #111 (chance meeting → planned meetup).
- **SR61:** place travel works (Dufus stops 38 cm from Don's). Found tick crash + #27 A.
- **Fix — tick crash:** `_not_ready_reason` used `agent.tick_interval_seconds` → `tick_interval`
  (crashed every other tick since 42fdd59).
- **Fix — #27 A:** `_observe_agent` treats stale `moving_to [x, y, z]` as `arrived` within
  `_ARRIVED_CM` = 100 cm (`agent_manager.py`, `_MOVING_TO_RE`). SR62: 0 stuck, 0 errors.
- **Feature — sim time on /sim:** `get_status()["world_time"]`; `sim.html` shows it top-right.
- **Qwen local (SR64):** `qwen3.5:4b` on Ollama timed out (120 s) on the RTX 4060 beside PIE. Parked.
- **Feature — #110 OpenRouter:** `_decide_openrouter` in `llm_router.py` (wake/decide w/ map
  image/ask/chat), `openrouter` branch in `perception.py`, both with `reasoning: {enabled: false}`.
  `provider_profiles.py` seeds `openrouter`/`openrouter-vision`; `Python/config.json` (gitignored)
  assigns profile `qwen-flash` to both roles so the /providers page matches `.env`.
- **SR65 (qwen):** Dufus did 08:00 Don's → 09:00 square → 10:00 truck; Maren held the truck.
- **Fix — #45:** `_has_unheard_speech` (peek, no cursor move) wakes both the mapped-place and scene
  gates (`why = "someone spoke within earshot"`); failed `speak_to` is not published;
  `known_characters` lists only active APCs (SR65 Dufus greeted a mannequin as "Surveyor").
- **Memory:** never push until user says; speed mode ON (no tests, test ideas → backlog ledger);
  mannequins are a feature; Play ladder.

## In flight

- **#45 fix** — code committed locally (`376f2e4`), not run live. Verify in SR66: after Maren
  speaks at the truck, the log shows `[dufus] someone spoke within earshot` and `[dufus] heard:`,
  then a Dufus reply, then Maren wakes on his reply.
- `plan/backlog.md` has the new #111 entry **uncommitted**.

## Decisions locked in this session

- Play ladder order: P0 travel → P1 #27 A → P2 #45 → P3 #67 talk into both memories → P4 small
  day → P5 #68/#106/3rd APC. Survey work is not restarted.
- **Provider = OpenRouter `qwen/qwen3.7-flash`** for decision and vision (user's pick) — cost.
  Back to Claude: /providers → `haiku` for both roles.
- **No push until the user says "push".** Local commits are fine.
- **Speed mode:** do not write or run tests; every fix adds a "needs a test" line in the backlog
  ledger (section "Needs tests (speed mode…)", 2026-09-22 block).
- **Mannequins** (static 3D people) are intentional; never filter VLM "unknown person" in code —
  record facts (spoke, no reply) and let the LLM conclude (#109).
- #27 A fixed Python-side only (100 cm radius). C++ move lifecycle only if stale moves reappear.

## Open questions

- Does qwen3.7-flash make good enough social decisions for #67 conversation? SR66 is the first look.
- OpenRouter dropped ~2 of 10 smoke calls and 1 vision call in SR65. Add one retry, or accept an
  idle tick? Not decided; watch the rate.
- #111 (user's vision: Dufus sees Maren leaving, walks over, "I'm off to Don's", Maren checks her
  schedule, they agree to meet at X o'clock, both show up): needs #67 plus an agreed-meeting →
  agenda write for both. Shape of that action not designed.
- 9 stale test scripts (#107) — fix when the user opens the test gate.

## Next concrete step

User runs SR66 (restart `Python/start_sim.bat`, Play, ≥15 min until Sim time passes 10:30); then
Claude reads `Python/worlds/MCP_World/logs/sim_runner.log` for `[SR66]` and checks whether Dufus
hears and answers Maren at the truck (the #45 fix), recording results in the backlog Active view.

## Files touched this session

- `MASTER_PLAN.md` — current state, Play ladder, orientation note.
- `plan/backlog.md` — Active view, SR61–SR65 results, #107–#111, needs-test ledger.
- `plan/handoffs/HANDOFF_2026-09-22_1200.md`, this file, `LATEST.md`.
- `Python/agent_runtime/agent_manager.py` — tick crash fix, #27 A arrival, world_time in status,
  #45 speech wake, success-only utterances, active-only known characters.
- `Python/agent_runtime/llm_router.py` — OpenRouter provider.
- `Python/agent_runtime/perception.py` — OpenRouter vision.
- `Python/agent_runtime/provider_profiles.py` — OpenRouter seed profiles.
- `Python/web_ui/templates/sim.html` — Sim time display.
- `Python/web_ui/templates/providers.html` — OpenRouter in hint text.
- Not in git: `Python/.env` (providers → openrouter, `LLM_MODEL`/`OPENROUTER_*MODEL` =
  `qwen/qwen3.7-flash`, `OPENROUTER_API_KEY` set by user), `Python/config.json` (roles → qwen-flash).
- Earlier in session: committed the Sept 9 place-travel slice (`b7b5dd3`).

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: user runs SR66 (≥15 min, past 10:30 sim time); read the `[SR66]` log and check whether Dufus hears and answers Maren at the truck (#45 fix).

## Git state at handoff

```
 M plan/backlog.md
?? plan/handoffs/HANDOFF_2026-09-22_1925.md
 M plan/handoffs/LATEST.md
```

Local `main` is 6 commits ahead of `origin/main` (not pushed, by user rule).

Recent commits:

```
376f2e4 fix: #45 unheard speech wakes settled APCs; failed speech not published; inactive APCs not known characters; docs: SR65
275d198 feat: #110 OpenRouter provider for decisions and vision (reasoning off), providers page seeds
4660d5c docs: SR64 Qwen timed out; #110 OpenRouter provider; back on Anthropic
27166b2 docs: SR62 - #27 A passed live, runs too short to reach 09:00, next SR63 on local Qwen
b106b21 docs: #109 mannequins - APCs learn who is fake by talking to them
```
